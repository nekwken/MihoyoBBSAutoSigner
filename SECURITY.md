# Security Policy

## 范围

本项目是**非官方**个人辅助工具，只处理：

- 读取/写入本地配置中的用户自有账号凭证（用户主动填入）
- 调用米哈游公开 HTTP 接口完成签到等用户发起的操作

**不在范围内（请勿提交此类 Issue）：**

- 多开、刷号、批量账号自动化
- 绕过服务端安全策略、破解加密
- 商业牟利或干扰服务的行为

## 凭证安全

- 勿在 Issue / PR / 讨论中粘贴 `cookie`、`stoken`、`ltoken`、`cookie_token`
- 勿提交 `config.yaml`、`tray_config.json`、`tray.log` 等含账号凭证的本地文件
- 疑似泄露时：立即在米游社/通行证**修改密码**并重新登录，使旧 token 失效

## 报告漏洞

若发现本仓库代码中的安全问题（例如日志误打印凭证、路径穿越），可通过 GitHub Security Advisories 或私信维护者报告。

**请不要**报告「米哈游接口校验过严导致本工具失效」——这不属于本项目的漏洞。
